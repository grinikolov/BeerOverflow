@model BeerOverflowAPI.Models.CountryViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Country</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Breweries)
        </dt>
        <dd class="col-sm-10">
            @string.Join(",",Model.Breweries)
        </dd>

                @*@foreach (var item in Model.Breweries)
                {
                    <dd class="col-sm-10">
                        @Html.DisplayFor(item => model.Breweries)
                    </dd>
                }*@
        </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
